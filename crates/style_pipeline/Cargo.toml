[package]
name = "style_pipeline"
version = "0.1.0"
edition = "2021"

[features]
default = []
plugin_svg_fallback = []
plugin_data_inline = []

[dependencies]
lightningcss = { version = "1.0.0-alpha.70", default-features = false, features = ["browserslist", "sourcemap"] }
parcel_sourcemap = "2.1.1"
serde_json = "1.0"
regex = "1.11"
